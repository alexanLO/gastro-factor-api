#==============================
# Configuracao local
#==============================
spring:
  config:
    activate:
      on-profile: local

#==============================
# JWT (somente ambiente local)
#==============================
jwt:
  secret: ${JWT_SECRET_LOCAL:minha_chave_super_secreta_com_mais_de_32_caracteres_123456}